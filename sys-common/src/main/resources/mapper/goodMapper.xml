<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.common.dao.GoodDao">
    <select id="getByCelebrityType" resultType="com.example.common.entity.Good">
        select *
        from goods
        where type = #{type}
        limit #{start}, 18;
    </select>

    <select id="get4" resultType="com.example.common.entity.Good">
        select *
        from goods
        where eshop_account = #{account}
        limit 0, 4;
    </select>

    <select id="getAll" resultType="com.example.common.entity.Good">
        select *
        from goods
        where eshop_account = #{account}
        limit #{start}, 10
    </select>

    <insert id="addGoods" parameterType="com.example.common.entity.Good">
        insert into goods (name, img, type,
                           market_price,
                           celebrity_price, brand,
                           intro, eshop_account)
        values (#{name}, #{img}, #{type},
                #{marketPrice}, #{celebrityPrice},
                #{brand}, #{intro},
                #{eshopAccount})
    </insert>

    <update id="updateStatus">
        update goods set status=#{status} where good_id=#{goodId}
    </update>


    <select id="getGoodId" resultType="int">
        SELECT good_id FROM goods WHERE type=#{type}
    </select>

    <select id="getGoodById" resultType="com.example.common.entity.Good">
        select * from goods where good_id=#{goodId}
    </select>

</mapper>