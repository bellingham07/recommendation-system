<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.common.dao.UserBehaviorDao">
    <update id="insertHobby">
        update user_behavior
        set like1=#{hobby1},
            like2=#{hobby2}
        where account = (#{account})
    </update>

    <insert id="save">
        insert into user_behavior (account)
        values (#{account})
    </insert>

    <update id="saveBehavior" parameterType="com.example.common.entity.UserBehavior">
        update user_behavior
        set browse1=#{userBehavior.browse1},
            browse2=#{userBehavior.browse2},
            browse3=#{userBehavior.browse3},
            browse4=#{userBehavior.browse4}
        where account = #{userBehavior.account}
    </update>

    <select id="all" resultType="com.example.common.entity.UserBehavior">
        select * from user_behavior where account=#{account}
    </select>
</mapper>